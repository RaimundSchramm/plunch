.center.jumbotron
  %p#notice= notice

  %h1 Meals

  %table
    %thead
      %tr
        %th Name
        %th Had it
        %th Lastly eaten on
        %th{:colspan=>'3'}
    %tbody
      - @meals.each do |meal|
        %tr
          %td.name= meal.dish.try(:name)
          %td.days= days_passed(Statistics.new.days_since_last_meal(meal.dish))
          %td.eaten= eaten_on_formatted(meal.eaten_on)
  %br/

  = link_to 'New Meal', new_meal_path